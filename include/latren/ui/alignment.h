#pragma once

namespace UI {
    enum class HorizontalAlignment {
        LEFT,
        RIGHT,
        CENTER
    };
    enum class VerticalAlignment {
        TOP,
        MIDDLE,
        BOTTOM
    };
};