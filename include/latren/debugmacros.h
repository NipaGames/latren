#pragma once

// some of these really shouldn't be macros but runtime options instead but i'm too lazy now

/* --- UI --- */
// #define LATREN_DEBUG_UI_BOUNDS

/* --- TEXT RENDERING --- */
// #define LATREN_FORCE_TEXT_RENDER_EVERY_FRAME
// #define LATREN_FORCE_TEXT_RENDER_TO_TEXTURE
// #define LATREN_DEBUG_TEXT_TEXTURES
// #define LATREN_DUMP_FONT_ATLAS_PNGS