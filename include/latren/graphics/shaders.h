#pragma once

namespace Shaders {
    enum class ShaderID {
        UNLIT,
        LIT,
        FRAMEBUFFER,
        HIGHLIGHT_NORMALS,
        UI_TEXT,
        UI_SHAPE,
        UI_BLINK,
        LINE,
        STROBE_UNLIT,
        SKYBOX,
        BILLBOARD
    };
};