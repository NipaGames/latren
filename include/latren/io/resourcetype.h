#pragma once

namespace Resources {
    enum class ResourceType {
        TEXTURE,
        SHADER,
        FONT,
        MODEL,
        STAGE,
        AUDIO,
        DATA,
        TEXT,
        BINARY,
        JSON,
        CFG
    };
};